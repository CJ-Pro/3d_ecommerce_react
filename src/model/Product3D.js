export default class Product3D {
  constructor(productId, alpha, beta, radius, target, url) {
    this.productId = productId;
    this.alpha = alpha;
    this.beta = beta;
    this.radius = radius;
    this.target = target;
    this.url = url;
  }
}
